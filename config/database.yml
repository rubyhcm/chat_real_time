default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: chat_dev

test:
  <<: *default
  database: chat_test

production:
  <<: *default
  url: <%= ENV['DATABASE_URL'] %>
